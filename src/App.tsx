import "./App.css";
import { ChefHat } from "lucide-react";
// lucide icons는 shadcn설치 시 기본적으로 설치되는 라이브러리로,
// import 후 컴포넌트 사용하는 것처럼 선언하면 브라우저에 svg형태로 그려지게 된다.
function App() {
  return <ChefHat />;
}

export default App;
